{% extends '/front/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <div class="bg-secondary flex py-20 px-40 mb-20">
        <h1 class="text-5xl font-bold mb-8">Je recherche ...</h1>
    </div>

    {{ component('Front:Filter:Filter') }}

    <div class="container px-20 mx-auto py-2 mb-20">
        {{ component('Front:Product:ProductListing', {listing : 'searchProducts', cols: 5 }) }}
    </div>

{% endblock %}