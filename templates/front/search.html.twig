{% extends '/front/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Front:Banner', { title: 'Je recherche ...' }) }}

    {% set items = [
        ['Recherche', 'app_front_search']
    ] %}
    {{ component('Front:Breadcrumb:Breadcrumb', { items: items }) }}

    {{ component('Front:Filter:Filter') }}

    <div class="container px-20 mx-auto py-2 mb-20">
        {{ component('Front:Product:ProductListing', {listing : 'searchProducts', cols: 5 }) }}
    </div>

{% endblock %}