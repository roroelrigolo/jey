{% extends '/front/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block javascripts %}

{% endblock %}

{% block body %}
    <div class="bg-secondary flex py-20 px-40 mb-20">
        <h1 class="text-5xl font-bold mb-8">{{ sport.title }}</h1>
    </div>

    {{ component('Front:Filter:Filter') }}

    {{ component('Front:Product:ProductListing', {listing : 'sportProducts', sport_id : sport.id }) }}
{% endblock %}

