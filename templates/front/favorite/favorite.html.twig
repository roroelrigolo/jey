{% extends '/front/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Front:Banner', { title: 'Mes favoris' }) }}

    {% set items = [
        ['Favoris', 'app_front_favorite']
    ] %}
    {{ component('Front:Breadcrumb:Breadcrumb', { items: items }) }}

    <div class="container mx-auto px-2 py-2 mb-8 lg:mb-20 2xl:px-20">
        {{ component('Front:Product:ProductListing', { user_id: app.user.id, listing : 'favoriteProducts', cols: 5 }) }}
    </div>
{% endblock %}

