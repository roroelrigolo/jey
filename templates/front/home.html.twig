{% extends '/front/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block javascripts %}

{% endblock %}

{% block body %}
    <div class="container-full">
        <div class="row">
            <div class="banner-home">
                
            </div>
        </div>
    </div>
    <div class="container-xxl py-2">
        <div class="row">
            <h1 class="fs-30 mb-2">Maillots populaires</h1>
        </div>
        <div class="row row-cols-5 d-flex">
            {% for product in products %}
                {% include 'front/assets/product/card.html.twig' with {
                    'product' : product
                } %}
            {% endfor %}
        </div>
        <div class="row">
            <h1 class="fs-30 mb-2">Derniers maillots</h1>
            {% for product in lastProducts %}
                {% include 'front/assets/product/card.html.twig' with {
                    'product' : product
                } %}
            {% endfor %}
        </div>
    </div>
{% endblock %}