<div class="flex justify-end mt-4">
    {% for button in buttons %}
        {{ component('Global:Button',{
            tag: button.tag,
            text: button.text,
            class: button.class
        }) }}
    {% endfor %}
</div>