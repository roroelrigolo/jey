{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Types de notifications' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer', id_entity: notification_type.id, type: 'notificationType' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/notification_type/_form.html.twig', form: form }) }}
{% endblock %}
