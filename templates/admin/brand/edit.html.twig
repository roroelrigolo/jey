{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Marques' }) }}
    {{ component('Admin:Form:Header', { title: 'Éditer' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/brand/_form.html.twig', form: form }) }}
{% endblock %}
