{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Avis & Réponses' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer avis', id_entity: assessment.id, type: 'assessment' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/assessment/_form.html.twig', form: form }) }}

    {% if reply.id is not null %}
        {{ component('Global:Form:Header', { title: 'Éditer réponse', id_entity: reply.id, type: 'reply' }) }}
    {% else %}
        {{ component('Global:Form:Header', { title: 'Éditer réponse' }) }}
    {% endif %}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/assessment/reply/_form.html.twig', form: form_reply }) }}
{% endblock %}
