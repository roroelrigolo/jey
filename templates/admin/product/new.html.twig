{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/product/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Produits' }) }}
    {{ component('Global:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/product/_form.html.twig', form: form }) }}
{% endblock %}
