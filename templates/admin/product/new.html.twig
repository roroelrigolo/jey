{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/product/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Produits' }) }}
    {{ component('Admin:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/product/_form.html.twig', form: form }) }}
{% endblock %}
