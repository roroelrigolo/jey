{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/product/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Produits' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/product/_form_edit.html.twig', form: form, product: product }) }}
{% endblock %}
