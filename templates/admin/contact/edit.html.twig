{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Demandes de contact' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer', id_entity: contact.id, type: 'contact' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/contact/_form.html.twig', form: form }) }}
{% endblock %}
