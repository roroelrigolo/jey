{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/user/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Utilisateurs' }) }}
    {{ component('Admin:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/user/_form.html.twig', form: form }) }}
{% endblock %}
