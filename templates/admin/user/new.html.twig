{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/user/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Utilisateurs' }) }}
    {{ component('Global:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/user/_form.html.twig', form: form }) }}
{% endblock %}
