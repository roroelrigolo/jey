{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Conversations' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer', id_entity: conversation.id, type: 'conversation' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/conversation/_form.html.twig', form: form, conversation:conversation }) }}
{% endblock %}
