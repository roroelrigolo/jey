{{ form_start(form, {'attr' : {'enctype': 'multipart/form-data'} }) }}
{% if app.request.attributes.get("_route") == 'app_admin_league_edit' %}
    {{ component('Global:Form:Page', {
        id: 'general',
        inputs: [
            {component:'Global:Form:Input',form:form,input_name:'title'},
            {component:'Global:Form:Checkbox',form:form,input_name:'sports',id_league: league.id,attributs:[{name:'class',value:''}]}
        ],
    }) }}
{% else %}
    {{ component('Global:Form:Page', {
        id: 'general',
        inputs: [
            {component:'Global:Form:Input',form:form,input_name:'title'},
            {component:'Global:Form:Checkbox',form:form,input_name:'sports'}
        ],
    }) }}
{% endif %}
{{ component('Global:Form:Buttons', {
    buttons: [
        { tag: 'input', text: 'Enregistrer', class:'primary' },
        { tag: 'input', text: 'Enregistrer & rester', class:'primary' }
    ]
}) }}
{{ form_end(form) }}