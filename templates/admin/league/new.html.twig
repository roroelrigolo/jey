{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Ligues' }) }}
    {{ component('Admin:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/league/_form.html.twig', form: form }) }}
{% endblock %}
