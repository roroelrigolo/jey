{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Signalements' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/alert/_form.html.twig', form: form }) }}
{% endblock %}
