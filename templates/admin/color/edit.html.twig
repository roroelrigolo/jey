{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/color/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Couleurs' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/color/_form.html.twig', form: form }) }}
{% endblock %}
