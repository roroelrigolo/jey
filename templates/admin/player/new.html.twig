{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/player/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Joueurs' }) }}
    {{ component('Global:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/player/_form.html.twig', form: form }) }}
{% endblock %}
