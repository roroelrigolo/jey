{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/player/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Joueurs' }) }}
    {{ component('Admin:Form:Header', { title: 'Éditer' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/player/_form.html.twig', form: form, player:player }) }}
{% endblock %}
