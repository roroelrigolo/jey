{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/player/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Joueurs' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer', id_entity: player.id, type: 'player' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/player/_form.html.twig', form: form, player:player }) }}
{% endblock %}
