{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <h2 class="fs-22 mb-4">Sports</h2>
    <div class="table-back mb-4">
        <div class="header-blue d-flex align-items-center">
            <a class="btn-gold-plein" href="{{ path('app_admin_sport_new') }}">Ajouter un sport</a>
            <a class="btn-gold-plein">Filtrer</a>
        </div>
        <div class="container w-100 py-3 px-5 m-0">
            {% include 'admin/table.html.twig' with {
                'titles': ['Id','Nom','Menu',''],
                'cols' : ['col-1','col-9','col-1','col-1'],
                'datas' : datas,
                'features' : ['','','','d-flex justify-content-end'],
                'url' : 'app_admin_sport_edit'
            }
            %}
        </div>
    </div>
{% endblock %}