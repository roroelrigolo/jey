{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/sport/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Sports' }) }}
    {{ component('Admin:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Admin:Form:Form', { form_link: '/admin/sport/_form.html.twig', form: form }) }}
{% endblock %}
