{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/sport/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Sports' }) }}
    {{ component('Global:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/sport/_form.html.twig', form: form }) }}
{% endblock %}
