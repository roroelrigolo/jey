{{ form_start(form, {'attr' : {'enctype': 'multipart/form-data'} }) }}
{{ component('Admin:Form:Page', {
    id: 'general',
    inputs: [
        {component:'Admin:Form:Input',form:form,input_name:'title'},
        {component:'Admin:Form:Input',form:form,input_name:'displayMenu',type:'toogle'}
    ],
}) }}
<div class="mb-3">
    <label class="w-100">Images</label>
    <input class="p-2 w-100" type="file" name="banner" id="banner" required>
</div>
{{ component('Admin:Form:Buttons', {
    buttons: [
        { tag: 'input', text: 'Enregistrer', class:'primary' },
        { tag: 'input', text: 'Enregistrer & rester', class:'primary' }
    ]
}) }}
{{ form_end(form) }}

