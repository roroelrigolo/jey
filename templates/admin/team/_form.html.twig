{{ component('Global:Form:Nav', {
    nav_items: [
        {id:'general',text:'Général'},
        {id:'a-propos',text:'A propos'},
    ],
}) }}
{{ form_start(form) }}
{{ component('Global:Form:Page', {
    id: 'general',
    inputs: [
        {component:'Global:Form:Input',form:form,input_name:'title'},
        {component:'Global:Form:Input',form:form,input_name:'available',type:'toogle'}
    ],
}) }}
{% if app.request.attributes.get("_route") == 'app_admin_team_edit' %}
    {{ component('Global:Form:Page', {
        id: 'a-propos',
        class: 'hidden',
        inputs: [
            {component:'Global:Form:Checkbox',form:form,input_name:'leagues', id_team: team.id, attributs:[{name:'class',value:''}]},
        ],
    }) }}
{% else %}
    {{ component('Global:Form:Page', {
        id: 'a-propos',
        class: 'hidden',
        inputs: [
            {component:'Global:Form:Input',form:form,input_name:'leagues'},
        ],
    }) }}
{% endif %}
{{ component('Global:Form:Buttons', {
    buttons: [
        { tag: 'input', text: 'Enregistrer', class:'primary' },
        { tag: 'input', text: 'Enregistrer & rester', class:'primary' }
    ]
}) }}
{{ form_end(form) }}

