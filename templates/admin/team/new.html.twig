{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    <script>{% include '/admin/team/js.js.twig' %}</script>

    {{ component('Admin:TitlePage', { title: 'Équipes' }) }}
    {{ component('Global:Form:Header', { title: 'Ajouter' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/team/_form.html.twig', form: form }) }}
{% endblock %}
