<form method="post" action="{{ path('app_admin_textil_delete', {'id': textil.id}) }}"
      onsubmit="return confirm('Voulez vous vraiment supprimer cette matière ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ textil.id) }}">
    {{ component('Global:Button',{
        tag: 'button',
        text: 'Supprimer',
        class: 'delete'
    }) }}
</form>
