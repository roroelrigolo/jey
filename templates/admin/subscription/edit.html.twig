{% extends '/admin/base.html.twig' %}

{% block title %}Jey{% endblock %}

{% block body %}
    {{ component('Admin:TitlePage', { title: 'Abonnements' }) }}
    {{ component('Global:Form:Header', { title: 'Éditer', id_entity: subscription.id, type: 'subscription' }) }}
    {{ component('Global:Form:FormAdmin', { form_link: '/admin/subscription/_form.html.twig', form: form }) }}
{% endblock %}
